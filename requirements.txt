# RedSea GPT - Python dependencies
# Install with:  pip install -r requirements.txt

# --- LangChain core + community integrations ---
langchain>=0.3.28
langchain-core>=0.3.28
langchain-community>=0.3.13
langchain-text-splitters>=0.0.4

# --- Embeddings & vector store ---
langchain-huggingface>=0.1.2
sentence-transformers>=2.2.2          # all-mpnet-base-v2 embedder
chromadb>=0.4.0                       # persistent vector store
langchain-chroma>=0.1.2

# --- PDF ingestion ---
pypdf>=3.12.0

# --- Numerics & retrieval utilities ---
numpy>=1.24.0
scikit-learn>=1.3.0                   # cosine similarity for MMR

# --- LLM transport (OptiLLM / Groq / OpenAI-compatible HTTP clients) ---
requests>=2.31.0

# --- Config & tooling ---
python-dotenv>=1.0.0                  # load .env without leaking keys
tqdm>=4.66.1
pytest>=7.0.0                         # test suite

# --- Optional: faster embedding model loading (uncomment if needed) ---
# accelerate>=0.20.3
